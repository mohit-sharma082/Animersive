const { StyleSheet, StatusBar } = require("react-native");

export default STYLES = StyleSheet.create({

    container: {
        flex: 1,
        padding: 10,
    },
    pageTitle: {
        fontSize: 20,
        paddingVertical: 10,
        padding: 5,
        color: '#fff',
        fontWeight: '600'
    },


})